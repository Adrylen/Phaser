var Upgrade = function(game) {
    // Constructor
};

Upgrade.prototype = {
    // Display
    display: function() {
        alert("Ok");
    },

    //Update server
    update: function() {
        /* ARTHUR */
    }
};

var upgrade = new Upgrade(game);
